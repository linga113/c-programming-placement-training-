#include<stdio.h>
int main(){
	int a,b,c;
	scanf("%d%d%d",&a,&b,&c);
	if(a>b&&a>c){
		printf("a is lager number:%d",a);
	}
	if(b>a&&b>c){
		printf("b is lager number:%d",b);
	}
	if(c>a&&c>b){
		printf("c is lager number:%d",c);
	}
	else
	{
		printf("all numbers are equal");
	}
}
